============
Installation
============

At the command line::

    $ easy_install spid-django

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv spid-django
    $ pip install spid-django
