Take care of this folder that could contain private key. Be sure that this folder never is published.

The SPID Demo require a certificate Python Toolkit expects that certs for the SP could be stored in this folder as:

 * demo.key     Private Key
 * demo.crt     Public cert

Also you can use other cert to sign the metadata of the SP using the:

 * metadata.key
 * metadata.crt